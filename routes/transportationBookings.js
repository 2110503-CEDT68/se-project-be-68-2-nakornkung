const express = require('express');
const router = express.Router();
const transportationsRouter = require('./transportations');
const {} = require('../controllers/transportationBookings');

module.exports = router;